import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  align-items: center;
  gap: 0.2rem;

  label {
    font-size: 14px;
    color: #84848d;
    margin-top: 0.2rem;
  }

  input {
    border-radius: 4px;
    accent-color: #ff0000;
  }
`;
