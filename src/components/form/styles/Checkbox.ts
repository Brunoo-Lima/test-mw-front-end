import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  align-items: center;
  gap: 0.2rem;

  label {
    font-size: 15px;
    color: #84848d;
  }

  input {
    border-radius: 4px;
    accent-color: #ff0000;

    border-color: #ff0000;
  }
`;
